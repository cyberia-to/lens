[package]
name = "cyber-lens-brakedown"
version = "0.1.1"
edition.workspace = true
license-file = "../LICENSE"
description = "Brakedown polynomial commitment — expander-graph codes over Goldilocks"
repository.workspace = true
rust-version.workspace = true
keywords = ["brakedown", "pcs", "zk", "crypto"]
categories = ["cryptography"]

[dependencies]
cyber-lens-core = { version = "0.1.1", path = "../core" }
nebu.workspace = true
cyber-hemera.workspace = true

[dev-dependencies]
postcard = { version = "1", features = ["alloc"] }

[features]
# Wire-tightness test (bit-flip scan) needs Opening: Serialize + Deserialize.
serde = ["cyber-lens-core/serde"]
