[package]
name = "cyber-lens-core"
version.workspace = true
edition.workspace = true
license-file = "../LICENSE"
description = "Lens trait, types, and transcript for polynomial commitment"
repository.workspace = true
rust-version.workspace = true
keywords = ["pcs", "zk", "crypto", "pcs"]
categories = ["cryptography"]
readme = "../README.md"

[dependencies]
strata-core.workspace = true
strata-proof.workspace = true
cyber-hemera.workspace = true
nebu.workspace = true

serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
serde_json = "1"
cyber-hemera.workspace = true

[features]
serde = ["dep:serde", "cyber-hemera/serde"]
