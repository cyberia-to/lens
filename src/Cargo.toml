# ---
# tags: lens, config
# crystal-type: source
# crystal-domain: comp
# ---
[package]
name = "cyb-lens"
version.workspace = true
edition.workspace = true
license-file = "../LICENSE"
description = "polynomial commitment: five lenses for five algebras"
repository.workspace = true
rust-version.workspace = true
keywords = ["pcs", "zk", "crypto", "pcs"]
categories = ["cryptography"]
readme = "../README.md"
exclude = ["../specs/", "../CLAUDE.md"]

[lib]
name = "lens"
path = "lib.rs"

[dependencies]
cyb-lens-core = { version = "0.1.1", path = "../core" }
cyb-lens-brakedown = { version = "0.1.1", path = "../brakedown" }
cyb-lens-binius = { version = "0.1.1", path = "../binius" }
cyb-lens-ikat = { version = "0.1.1", path = "../ikat" }
cyb-lens-assayer = { version = "0.1.1", path = "../assayer" }
cyb-lens-porphyry = { version = "0.1.1", path = "../porphyry" }

[dev-dependencies]
nebu.workspace = true
kuro.workspace = true
trop.workspace = true
genies.workspace = true
cyber-hemera.workspace = true
